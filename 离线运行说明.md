# 网络攻防指挥控制决策系统 · 前端（React）离线运行说明

大模型 + 智能体 + 本体驱动的多局域网网络攻防指挥控制决策系统 · 演示前端。
基于 **React 19 + Vite + TypeScript**，业务数据全部来自 `src/core/data.js`（虚拟实验环境样例），**运行时不请求任何后端接口**。

## 三种打开方式

| 方式 | 命令 / 操作 | 说明 |
|---|---|---|
| 开发调试 | `npm run dev` | http://localhost:3000 |
| 离线单文件（推荐演示） | 双击 `打开离线版.command` | 自动判断：源码更新则重建，然后打开 |
| 手动构建离线版 | `npm run build:offline` | 产物 `out-offline/index.html`，自包含，可拷贝分发，双击即开 |

## 目录结构

```
src/
  main.tsx            入口：挂载 React、确定初始 hash 路由
  App.tsx             应用外壳：路由表（state.route→页面组件）+ 布局 + 全局浮层
  styles.css          全局样式（指挥大屏暗色主题，CSS 变量）
  core/
    data.js           虚拟实验环境数据：DB/SCEN（4 事件剧本）/ONTO/ONTO_CQS/ONTO_ALIGN/GRAPH
    store.js          状态仓库：state/RT + 订阅通知（useSyncExternalStore）、RBAC、导航、页面标题
    utils.js          工具：时钟/审计/toast/侧栏面板/KPI 数字滚动
  actions/            业务动作层（修改 state/RT 后 notify()，不碰 DOM）
    system.js         导航/RBAC/治理配置/故障演练/一键演示脚本/环境重置
    pipeline.js       闭环动作：研判/规划/审批/DAG 编排/执行/验证/案例沉淀
    topology.js       网络拓扑动作：网络域/资产/手动关系/攻击路径推演
    knowledge.js      知识动作：本体/知识图谱/图谱构建/知识库/案例复盘
  pages/              16 个页面组件（纯 JSX，真实事件绑定）
  viz/                SVG 可视化组件（useEffect+ref 命令式绘制，数据签名驱动重绘）
    TopologySVG.jsx   网络拓扑（网格分区 + 攻击路径动画 + 平移缩放）
    DagSVG.jsx        DefenseTask DAG（分层布局 + 依赖编辑）
    OntoSVG.jsx       本体类关系图（六大类径向聚类力导向）
    KgSVG.jsx         知识图谱实例图（L1→L4 跨列力导向 + 焦点子图）
    panzoom.jsx       画布平移缩放引擎（每个 svg 独立 viewBox 记忆）
  ui/
    common.jsx        共享组件：徽章/标签页/闭环进度条/上下文条/Toast/侧栏宿主/页面外壳
    panels.jsx        侧栏面板：事件/资产/任务/能力问题/本体向导/图谱向导/案例复盘等表单
  components/
    Nav.tsx           侧边导航（分组 + 实时徽标）
    TopBar.tsx        顶栏：页面标题 / 登录角色（RBAC）/ LLM 状态 / 一键演示 / 时钟
    DemoBanner.tsx    一键演示横幅（state.demo 驱动）
```

## 架构说明（重要）

- **渲染方式**：标准 React 组件树。16 个业务页面全部为 JSX 组件（`src/pages/`），
  事件处理为真实 `onClick`/`onChange` 绑定；仅 SVG 可视化（拓扑/DAG/图谱/本体）
  保留命令式绘制（`useEffect`+`ref`+`createElementNS`），以「数据签名」字符串作为
  effect 依赖——只有相关数据变化才整图重绘，节点拖拽直接改 DOM 不触发重渲染。
- **路由**：hash 路由（`#dashboard` … `#governance`），离线 `file://` 与 dev server 行为一致，
  支持 `#kg` 等直达链接。
- **状态**：`core/store.js` 的 `state`/`RT` 为唯一状态源；业务动作（`src/actions/`）修改状态后
  调用 `notify()`，组件经 `useApp()`（useSyncExternalStore）订阅重渲染。
  无 `dangerouslySetInnerHTML` 页面注入、无 `window` 全局函数桥接。
- **RBAC**：右上角切换登录角色（指挥员/分析员/管理员/审计员），越权操作被 `needPerm`
  拦截并写入审计链。
- **离线约定**：不引入任何 CDN 资源；新增静态资源放 `src/assets/` 用 import 引入（会被内联）。

## 演示直达链接（打开 out-offline/index.html 后追加 hash）

- `#dashboard` 态势总览 ｜ `#topology` 网络拓扑 ｜ `#events` 事件中心
- `#analysis` 智能研判 ｜ `#planning` 方案规划 ｜ `#approval` 审批中心
- `#tasks` 任务编排 ｜ `#execution` 执行监控 ｜ `#gateway` 工具网关 ｜ `#effect` 效果验证
- `#ontology` 本体建模 ｜ `#kg` 知识图谱 ｜ `#graphbuild` 图谱构建 ｜ `#kb` 知识库 ｜ `#cases` 案例库
- `#governance` 系统治理（流程配置 / 大模型配置 / 角色与权限）

## 演示动线建议

1. 右上角「▶ 一键演示闭环」跟随 INC-20260918-01 走完整闭环（事件→研判→规划→审批→编排→执行→验证→案例）。
2. 右上角角色切换到「审计员」点击操作按钮，观察 RBAC 拦截与审计记录。
3. 切换「系统管理员」，在 系统治理→闭环流程配置 调整「高影响双人审批」，切回指挥员体验审批变化。
4. 点击顶栏「● LLM 在线」演练故障降级（Fail Closed 路径）。
